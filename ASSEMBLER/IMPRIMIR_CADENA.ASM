DATOS SEGMENT ; DEFINICIÓN DE SEGMENTO DE DATOS
    NOMBRE DB 'ALEJANDRO$'   ; CADENA, '$'--> PARA MARCAR EL FIN DE LA CADENA
DATOS ENDS    ; FIN DE SEGMENTO DE DATOS

PILA SEGMENT STACK "STACK" ; DEFINICIÓN DE SEGMENTO DE PILA
PILA ENDS ; FIN DE SEGMENTO DE PILA

CODE SEGMENT "CODE" ; DEFINICIÓN DE SEGMENTO DE CODIGO
    ASSUME CS:CODE,DS:DATOS,SS:PILA
    START PROC FAR ; INICIO DEL PROCEDIMIENTO PRINCIPAL START 
        MOV AX,DATOS      ; MUEVE EL SEGMENTO DE DATOS A AX
        MOV DS,AX         ; MUEVE A DS:=AX
        ; IMPRIMIR
        MOV AH,9H        
        LEA DX,NOMBRE
        INT 21H
    START ENDP 
CODE ENDS ; FIN DE SEGMENTO DE CODIGO 

END START